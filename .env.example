# Telegram API Credentials
# Get these from https://my.telegram.org/apps
TELEGRAM_APP_ID=your_app_id_here
TELEGRAM_APP_HASH=your_app_hash_here

# Optional: Session file location (defaults to ~/.local/share/tgcryptfs/session)
# TELEGRAM_SESSION_PATH=/path/to/session/file

# Optional: Mount point for the filesystem (can also be specified via CLI)
# TGCRYPTFS_MOUNT=/path/to/mount/point

# Optional: Log level (trace, debug, info, warn, error)
# RUST_LOG=info
